apiVersion: v1
kind: Service
metadata:
  name: pen-and-paper-dm-helper-db-service
spec:
  selector:
    app: pen-and-paper-dm-helper
    role: db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
  